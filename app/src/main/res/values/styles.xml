<resources>

    <!-- Base application theme. -->
    <style name="KilledTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <!--<item name="android:windowBackground">@drawable/app_background</item>-->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/dialog_edge</item>
        <item name="android:windowAnimationStyle">@style/DialogAnim</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:colorBackground">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@android:color/black</item>
    </style>
    <style name="DailogBackground">
        <item name="android:windowBackground">@drawable/brown5</item>
    </style>
    <style name="DialogAnim">
        <item name="android:windowEnterAnimation">@anim/anim_bounce</item>
    </style>
    <style name="LayoutAnim">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

</resources>
